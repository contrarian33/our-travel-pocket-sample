# Our Travel Pocket 실험판 개발 규칙

이 규칙은 저장소 전체와 향후 모든 Codex 개발 세션에 적용한다.

## 범위와 작업 단위

1. 한 번에 [BACKLOG.md](./BACKLOG.md)의 WP 하나만 구현하고 다음 WP를 선행하지 않는다.
2. 시작 전 [PRD.md](./PRD.md), [ARCHITECTURE.md](./ARCHITECTURE.md), 이 파일, 대상 WP와 `git status --short`를 확인한다.
3. 한 명·한 기기·같은 브라우저·단일 활성 여행 실험판 범위를 유지한다.
4. 사용자 계정, 인증 화면, 서버 데이터 저장, 데이터베이스, ORM, 소유권 정책, 동기화 기능을 임의로 추가하지 않는다.
5. 기존 범위를 확장하지 않는다. 구현을 좌우하는 TBD는 작업 전에 사용자에게 질문한다.

## 데이터와 계산 규칙

1. localStorage의 v1 키는 `our-travel-pocket:v1`이며 읽을 때마다 전체 데이터를 Zod로 검증한다.
2. 서버 렌더링, 모듈 최상위와 클라이언트 마운트 전에는 `window` 또는 `localStorage`에 접근하지 않는다.
3. 저장 DTO는 JSON 호환 값만 포함한다. `bigint`를 localStorage JSON에 직접 저장하지 않는다.
4. 금액은 KRW 원, JPY 엔, USD 센트의 양의 정수 문자열로 저장한다. 계산 경계에서만 `bigint`로 변환하고 부동소수점 `number`로 계산하지 않는다.
5. USD 화면 입력은 최대 소수점 둘째 자리까지 허용해 센트 문자열로 정확히 변환한다. KRW와 JPY는 정수만 허용한다.
6. 공동 경비 생성 시 현재 전체 일행 ID를 `participantIds`로 스냅샷하고 수정 시 유지한다. 이후 추가 일행을 과거 경비에 넣지 않는다.
7. 결제자 또는 `participantIds`로 참조 중인 일행 삭제를 차단한다. 일부 일행 선택 기능을 만들지 않는다.
8. 개인 지출은 목록에 표시하되 집계와 정산에서 제외한다.
9. 통화를 섞거나 환율을 적용하지 않고 최대 10명 범위에서 정확한 최소 송금 건수를 계산한다.
10. 손상 데이터나 저장 실패를 조용히 덮어쓰지 않고 한국어 오류와 허용된 초기화 동작을 표시한다.

## 구현 품질과 검증

1. TypeScript strict를 유지하고 `any`나 무근거 단언으로 타입 오류를 숨기지 않는다.
2. 모바일 360px, 터치 조작, 한국어 레이블·오류·확인·포커스를 우선한다.
3. 관련 없는 사용자 변경을 수정하거나 되돌리지 않는다.
4. 구현 후 `npm run lint`, `npm run typecheck`, `npm run test`, `npm run build`를 실행한다.
5. Playwright 구성 이후 관련 작업에는 `npm run test:e2e`도 실행한다.
6. 실패한 검증을 성공으로 보고하지 않고 명령, 원인과 미검증 범위를 명시한다.
7. 완료 보고에는 WP, 변경 파일, 핵심 결정, 검증 결과, 남은 TBD와 `git status --short`를 포함한다.
8. 사용자 승인 없이 커밋, 푸시 또는 Vercel 배포를 수행하지 않는다.

