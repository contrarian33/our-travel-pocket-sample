# Our Travel Pocket 실험판 PRD

## 문서 기준

- 목표 사용일: 2026년 8월 28일 금요일
- 상태: planning
- 원본: Manyfast `My travel pocket` (`94af239c-13cd-4bf5-bd4f-1628f10812bd`)
- 이 실험판 확정 정책이 기존 정식 MVP 설계와 원문보다 우선한다.

## 목적과 검증 가설

한 명의 사용자가 한 대의 휴대폰과 같은 브라우저에서 별도 계정이나 설치 없이 여행 경비를 기록하고, 공동 경비 집계와 통화별 정산을 현장에서 이해하고 사용할 수 있는지 검증한다. 하나의 활성 여행만 관리하며 데이터는 해당 브라우저의 localStorage에만 보존한다.

## 핵심 사용자와 환경

- 경비를 대표로 입력하는 한 명의 사용자
- 한 대의 휴대폰과 같은 모바일 브라우저
- 계정이나 접근 권한 없이 표시 이름으로만 등록되는 일행
- 네트워크 저장 없이 브라우저 안에서 완결되는 핵심 경비 흐름

## 핵심 사용자 흐름

1. 저장 데이터가 없으면 여행 이름, 시작일, 종료일과 선택 국가를 입력한다.
2. 데이터 유실 위험 안내를 확인하고 여행을 시작한다.
3. 최대 10명의 일행을 중복되지 않는 표시 이름으로 등록한다.
4. 공동 경비 또는 개인 지출을 날짜, 항목명, 금액, 통화와 결제자로 기록한다.
5. 공동 경비 저장 시점의 전체 일행 ID를 `participantIds`로 저장한다. 이후 추가된 일행은 과거 경비를 부담하지 않는다.
6. 전체·준비 기간·특정 날짜 필터로 경비를 확인하고 수정하거나 확인 후 삭제한다.
7. 결제자·통화별 집계, 통화별 최소 송금과 항목별 분할 근거를 즉시 확인한다.
8. 필요할 때 확인 후 실험판 데이터를 전부 초기화한다.

## 기능 요구사항

### 단일 활성 여행

- 여행은 한 번에 하나만 저장한다. 여행 목록과 여행 전환은 없다.
- 여행 이름·시작일·종료일은 필수, 국가는 선택이며 종료일은 시작일보다 빠를 수 없다.
- 여행 정보를 확인하고 수정할 수 있다. 기간 수정 시 준비 기간 분류를 즉시 다시 계산한다.

### 일행 관리

- 일행을 계정 없는 표시 이름으로 추가·조회·수정·삭제한다.
- 같은 여행에서 정규화된 표시 이름 중복을 금지하고 최대 10명까지 허용한다.
- 공동 경비 등록에는 한 명 이상의 일행과 그중 한 명인 결제자가 필요하다.
- 경비의 결제자 또는 `participantIds`로 참조되는 일행은 해당 경비가 존재하는 동안 삭제할 수 없다.
- 일부 일행만 선택하는 기능은 제공하지 않는다.

### 경비 관리

- 항목명, 필수 날짜, 금액, KRW/JPY/USD, 결제자, 공동/개인 구분으로 등록한다.
- 공동 경비는 생성 시점 전체 일행 ID를 `participantIds` 스냅샷으로 저장한다.
- 공동 경비 수정 시 다른 필드는 바꿀 수 있지만 기존 `participantIds`는 유지한다.
- 개인 지출의 `participantIds`는 빈 배열이며 목록에는 표시하되 집계·분할·정산에서 제외한다.
- 시작일 이전 경비는 준비 기간으로 분류한다.
- 경비를 수정할 수 있고 삭제 전 대상과 영향을 확인한다.
- 경비 변경 후 집계와 정산을 즉시 다시 계산한다.

### 금액과 정산

- KRW와 JPY는 화면에서 양의 정수로 입력한다.
- USD는 화면에서 양수이며 최대 소수점 둘째 자리까지 입력한다.
- 저장 금액은 최소 단위 정수 문자열이다. KRW는 원, JPY는 엔, USD는 센트이며 USD `12.34`는 `"1234"`로 저장한다.
- JSON에 `bigint`를 직접 저장하지 않고 계산할 때만 검증된 문자열을 `bigint`로 변환한다.
- 공동 경비는 저장된 `participantIds`가 균등 분담하고 나머지 최소 단위는 결제자 부담에 더한다.
- 개인 지출을 제외하고 통화별로 정확한 최소 송금 건수를 계산한다.
- 서로 다른 통화를 합산·상계하거나 환율 변환하지 않는다.

### 로컬 저장과 초기화

- 저장 키는 `our-travel-pocket:v1`이며 모든 읽기 결과를 Zod로 다시 검증한다.
- 데이터가 없으면 초기 여행 설정을 표시한다.
- 데이터가 손상되면 앱을 중단하지 않고 한국어 오류와 전체 초기화 선택지를 표시한다.
- 정상 데이터는 새로고침과 같은 브라우저 재실행 후에도 유지된다.
- 서버 렌더링 중 브라우저 API에 접근하지 않고 클라이언트 마운트 후 불러온다.
- 초기 설정과 여행 정보 화면에 브라우저 데이터 삭제, 시크릿 모드, 다른 기기에서는 데이터가 유지되지 않고 백업·복구가 없음을 안내한다.
- 전체 초기화는 복구 불가 안내와 확인 후 저장 키를 제거한다.

## 필수 화면

### 1. 초기 여행 설정

- 저장 데이터 확인 전 중립 로딩 상태를 표시한다.
- 여행 필드, 데이터 유실 경고, 시작 동작을 한 손 사용에 맞게 배치한다.

### 2. 여행 정보 확인·수정

- 현재 정보와 데이터 유실 경고를 표시하고 이름, 국가와 기간을 수정한다.

### 3. 일행 목록·추가·수정·삭제

- 현재 인원수와 10명 제한을 표시한다.
- 중복·빈 이름·최대 인원 오류를 입력 가까이에 표시한다.
- 참조 중 삭제를 차단하고 관련 경비가 존재함을 안내한다.

### 4~7. 경비 목록·등록·수정·확인 삭제·필터

- 목록에 항목명, 날짜/준비 기간, 화면용 금액, 통화, 결제자와 공동/개인 구분을 표시한다.
- 전체, 준비 기간, 특정 날짜 필터를 제공한다.
- 모바일 숫자 키보드에 맞는 금액 입력을 제공한다.
- 최소 단위 저장 문자열을 화면 통화 표기로 정확히 되돌린다.
- 삭제 확인 전에는 데이터를 변경하지 않는다.

### 8. 결제자·통화별 공동 경비 집계

- 개인 지출을 제외하고 결제자와 통화별 합계 및 구성 항목을 표시한다.
- 통화 간 총합은 표시하지 않는다.

### 9~10. 통화별 정산 결과·항목별 분할 근거

- 송금자, 수령자, 금액과 통화를 정확한 최소 건수로 표시한다.
- 각 공동 경비의 총액, 스냅샷 인원, 기본 몫, 결제자 나머지와 일행별 부담을 표시한다.
- 경비 변경 직후 최신 결과를 표시한다.

### 11. 전체 데이터 초기화 확인

- 전체 여행·일행·경비가 복구 불가능하게 제거됨을 알리고 취소와 확인을 제공한다.

## 유효성 검증

- 필수 문자열은 앞뒤 공백 제거 후 비어 있을 수 없다.
- 날짜는 유효한 달력 날짜이고 종료일은 시작일 이상이다.
- 일행 이름은 정규화 후 유일하고 일행 수는 10명을 넘을 수 없다.
- 결제자는 현재 일행이며 공동 경비 생성 시 `participantIds`에 포함된다.
- 공동 경비의 `participantIds`는 생성 시점 전체 일행 ID와 정확히 일치하고 중복이 없다.
- KRW/JPY 입력은 자릿수만, USD는 최대 소수 둘째 자리까지 허용하며 금액은 0보다 크다.
- 저장 금액은 부호 없는 10진 정수 문자열이다.
- **TBD:** 여행/일행/항목명의 최대 길이와 금액 상한. WP-02 전에 확정해야 한다.
- **TBD:** 일행 이름의 대소문자, 연속 공백과 유니코드 정규화 규칙. WP-02 전에 확정해야 한다.

## 빈 상태와 오류 상태

- 데이터 없음: 초기 설정
- 일행 없음: 일행 추가 안내와 경비 등록 비활성/안내
- 경비 없음: 첫 경비 추가 동작
- 필터 결과 없음: 필터 해제 동작
- 공동 경비 없음: 집계·정산 대상 없음
- 송금 필요 없음: 정상 정산 완료 상태
- 손상/미지원 데이터: 원본을 렌더링하지 않고 오류와 전체 초기화 선택지
- 저장 실패/용량 초과: 성공으로 오인하지 않게 실패와 재시도 안내

## 수용 기준

1. 첫 방문과 전체 초기화 후 초기 설정이 나타나고 하나의 여행만 `our-travel-pocket:v1`에 저장된다.
2. 새로고침과 같은 브라우저 재실행 후 Zod 검증 데이터가 복원되고 hydration 오류가 없다.
3. 손상 JSON, 미지원 버전과 불완전 데이터에서도 앱이 중단되지 않고 초기화를 제공한다.
4. 일행은 최대 10명이고 중복 이름은 거부된다.
5. 공동 경비 생성 시 전체 일행 ID 스냅샷이 저장되고 이후 추가 일행은 과거 경비를 부담하지 않는다.
6. 결제자 또는 스냅샷에서 참조되는 일행은 관련 경비 삭제 전까지 삭제되지 않는다.
7. KRW/JPY 정수와 USD 최대 2자리 입력이 원/엔/센트 정수 문자열로 정확히 왕복된다.
8. localStorage JSON에 `bigint`가 없고 계산 경계에서만 문자열을 `bigint`로 변환한다.
9. 개인 지출은 목록에 보이지만 집계·분할·정산을 바꾸지 않는다.
10. 공동 경비 부담 합은 총액이고 나머지는 결제자에게 배정된다.
11. 통화별 송금 후 잔액이 모두 0이고 결과 건수는 전역 최소이며 통화가 섞이지 않는다.
12. 경비 생성·수정·삭제 후 집계와 정산이 즉시 갱신된다.
13. 시작일 전 경비와 전체·준비 기간·특정 날짜 필터가 정확하다.
14. 전체 초기화와 경비 삭제는 확인 전에 실행되지 않는다.
15. 주요 흐름을 360px 모바일 뷰포트에서 가로 스크롤 없이 한국어로 완료한다.

## 제외 범위

- 사용자 계정, 인증, 소유권과 권한 모델
- 서버 API와 서버 데이터 저장
- 관계형 데이터베이스와 ORM
- 여러 여행, 여러 기기 동기화와 여러 사용자 공동 편집
- 백업, 복구와 데이터 내보내기
- 일부 일행 선택 분할과 개인별 상이한 비율
- 영수증 이미지, 카테고리, 메모, 검색, 페이지네이션
- 환율 변환과 통화 간 합산
- 실제 송금과 송금 완료 관리

## 비기능 요구사항

- 모바일과 터치 조작을 최우선으로 하며 360px 너비를 지원한다.
- TypeScript strict를 유지하고 금액 계산에 부동소수점 `number`를 사용하지 않는다.
- 사용자 데이터를 네트워크로 전송하지 않는다.
- 명시적 레이블, 키보드 포커스, 오류 연결과 충분한 터치 영역을 제공한다.
- 최대 10명에서 정산과 화면 갱신이 체감상 즉시 완료되어야 한다. **TBD:** 자동화할 성능 임계값.

